/*
 * @lc app=leetcode.cn id=9 lang=javascript
 *
 * [9] 回文数
 */

// @lc code=start
/**
 * @param {number} x
 * @return {boolean}
 */
var isPalindrome = function(x) {
    // 简单粗暴
    // if (x < 0) return false
    // let str = `${x}`.split('')
    // return str.join('') === str.reverse().join('')

    // TODO: 不用字符串解决
    if (x < 0) return false
};
// @lc code=end

